import { within, userEvent, expect } from '@storybook/test';

import { HomePage } from './HomePage';

export default {
  title: 'PageComponents/HomePage',
  component: HomePage,
  parameters: {
    layout: 'fullscreen',
  },
};
